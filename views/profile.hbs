<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="/main.css">
  <title>Document</title>
</head>
<body>
<div class="container">
  <img class="img-circle" src="{{giver.pic}}" alt="{{giver.name}}">
  <h2 class="profileName">{{giver.name}}... {{act.tasks}} {{target.name}}</h2>
  <img class="img-circle" src="{{target.pic}}" alt="{{target.name}}"></div>
  <br>
  <br>
  <br>
  <div class="container">
  <p>Goodluck on your quest... Try to complete your task in secret</p>
  </div>
<div class="container">
  <form action="/profile/{{giver.id}}/done" method="POST" class="checkbox">
  <input type="hidden" name="giver" value="{{giver.name}}">
  <input type="hidden" name="act" value="{{act.tasks}}">
  <input type="hidden" name="target" value="{{target.name}}">
  <input type="hidden" name="targetPic" value="{{target.pic}}">
  <input type="checkbox" name="completed" value="completed"> Completed<br>
  <input type="checkbox" name="caught" value="Caught"> I got caught<br>
  <p> </p>
  <button type="submit">Submit</button>
</form>
  </div>
<br>
<div class="container">
  <button onclick='href=location.reload()'action="/profile/{{giver.id}}">Generate new task</button>
</div>
<br>
<br>
<br>
</body>
</html>
